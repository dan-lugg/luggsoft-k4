package com.luggsoft.k4.core.engine

class DefaultLocation(
    name: String,
    startIndex: Int,
    untilIndex: Int
) : LocationBase(
    name = name,
    startIndex = startIndex,
    untilIndex = untilIndex
)
